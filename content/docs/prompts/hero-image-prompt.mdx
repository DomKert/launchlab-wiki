---
title: 18. Hero image prompt
description: Execution. Day 1 PM. Run in Claude Project. Output fans out to Gemini Nano Banana, ChatGPT, and Ideogram in parallel.
---

<div className="mb-4 flex flex-wrap items-center gap-2">
  <Badge variant="tier-a">A</Badge>
  <Badge variant="side-quest">EXECUTION</Badge>
  <Badge variant="quest">DAY 1 PM</Badge>
</div>

<div className="my-4 rounded-md border border-fd-border bg-fd-card p-4 text-sm">
  <div className="mb-2 flex items-center gap-2"><Badge variant="loot">TOOL</Badge><span className="font-mono text-xs uppercase tracking-wider text-fd-muted-foreground">primary</span></div>
  <a href="https://claude.ai" target="_blank" rel="noreferrer" className="font-sans font-semibold no-underline text-emerald-600 dark:text-emerald-400 hover:underline">→ Claude Project</a>
  <div className="mt-3 text-xs uppercase font-mono tracking-wider text-fd-muted-foreground">Output fans out to all three (parallel runs)</div>
  <div className="mt-1 flex flex-wrap gap-3">
    <a href="https://gemini.google.com" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ Gemini Nano Banana</a>
    <a href="https://chatgpt.com" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ ChatGPT (gpt-image-2)</a>
    <a href="https://ideogram.ai" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ Ideogram</a>
  </div>
</div>

## Why this works

You don't need to spec a hero image like a creative director. Your 4-box already says who the customer is, what the offer is, and what voice the brand has. Claude reads it, makes the calls (mood, palette, photo style, composition), and outputs one image-gen prompt. You paste that one prompt into all three image tools (Gemini Nano Banana, ChatGPT, Ideogram) in parallel and bring the three options back to Claude for the pick. Same prompt, three different model "brains," different rendering each time. The fan-out is what gets you a hero you'd actually live with.

The prompt below is short on purpose. No mood adjective banks, no hex pickers, no curated palettes. Those create decisions where there shouldn't be any.

This runs in Claude Project. The OUTPUT goes to all three image tools.

## Before you run this

<div className="my-4">
  <PersistentCheckbox id="hero-prereq-1" label="Have your 4-box ready (in Project knowledge or pasteable)" />
  <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">From <a href="/docs/prompts/four-box" className="text-emerald-600 hover:underline dark:text-emerald-400">Prompt 2</a>. Already in Project knowledge if you saved it there.</p>
</div>

## Prompt

```txt
I want a hero image for my landing page. Read 4-box.md from this Project and synthesize a single image-gen prompt I'll paste into Gemini Nano Banana, ChatGPT (gpt-image-2), and Ideogram in parallel.

Pick mood, palette, photo style, and composition yourself based on what fits the niche. The 4-box has the customer, the offer, and the voice; that's enough to design from. Don't ask me about hex codes or photo-style names.

Ask me ONE question only if there's a real fork. The usual one is "people in the frame, or object-only?" If the answer is obvious from the 4-box, skip the question and decide.

Avoid SaaS-marketing slop: no laptops, no abstract gradients with floating UI cards, no people pointing at screens. Treat it like the cover of an editorial magazine for the niche, not a stock-photo site.

Output the final image-gen prompt as ONE code block. I'll generate one image per tool (3 options total), bring them back here, and ask you to critique against the brief.

Don't recommend a single tool. The fan-out across three is the point.
```

## After Claude outputs the image prompt

<div className="my-4 space-y-3">
  <div>
    <PersistentCheckbox id="hero-after-1" label="Step 1. Open all three image tools in tabs" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground"><a href="https://gemini.google.com" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">Gemini Nano Banana</a> + <a href="https://chatgpt.com" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">ChatGPT (gpt-image-2)</a> + <a href="https://ideogram.ai" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">Ideogram</a>. All three tabs at once.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-2" label="Step 2. Paste the same image-gen code block into each tab, one message per tab" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">No follow-up commentary in any of them. Just the prompt, then generate. Same input, three model brains, three different renderings.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-3" label="Step 3. Bring all three options back to your Claude Project chat" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Attach all three images to the same chat where you synthesized the prompt. Ask: "Critique these against the brief. Which matches the mood and composition cleanest? Which fails and why?" Claude has the brief context, so the critique is grounded.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-4" label="Step 4. Pick the winner, save next to index.html" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Trust Claude's pick unless you have a strong gut reason to override. Save the chosen file inside the folder you'll use for your landing page (so it sits next to <code>index.html</code> when you build the page in Prompt 17). The losers archive; some become Day 2 distribution image candidates.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-5" label="Step 5. Hand off to Prompt 17" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">When you run <a href="/docs/prompts/bolt-build" className="text-emerald-600 hover:underline dark:text-emerald-400">Prompt 17 Build prompt</a>, tell the synthesizer chat you have a hero image file ready. For Path A (ChatGPT), you'll reference it as a relative path in the HTML. For Path C (Bolt), you'll attach it via the paperclip.</p>
  </div>
</div>

## What good looks like

One Claude chat, 1 to 2 turns, image-gen prompt in one code block. Three parallel runs (Gemini + ChatGPT + Ideogram), three options compared back in Claude, winner saved to disk under 10 minutes.

## What bad looks like

Skipping the fan-out, generating from one tool only, getting a generic stock-photo result, then burning 5 follow-up runs trying to fix it with adjective piles. The fan-out IS the variance reducer; one tool is one perspective. The fix when a render misses is upstream too: change ONE input (subject, composition, photo style) and re-run, don't pile descriptors.

## Next

Hero image file saved to disk, run [Prompt 17 Build prompt](/docs/prompts/bolt-build). Claude will ask for it when synthesizing the build prompt.
