---
title: 18. Hero image prompt
description: Execution. Day 1 PM. Run in Claude Project. Synthesizes an image-gen prompt for Gemini Nano Banana or gpt-image-2.
---

<div className="mb-4 flex flex-wrap items-center gap-2">
  <Badge variant="tier-a">A</Badge>
  <Badge variant="side-quest">EXECUTION</Badge>
  <Badge variant="quest">DAY 1 PM</Badge>
</div>

<div className="my-4 rounded-md border border-fd-border bg-fd-card p-4 text-sm">
  <div className="mb-2 flex items-center gap-2"><Badge variant="loot">TOOL</Badge><span className="font-mono text-xs uppercase tracking-wider text-fd-muted-foreground">primary</span></div>
  <a href="https://claude.ai" target="_blank" rel="noreferrer" className="font-sans font-semibold no-underline text-emerald-600 dark:text-emerald-400 hover:underline">→ Claude Project</a>
  <div className="mt-3 text-xs uppercase font-mono tracking-wider text-fd-muted-foreground">Output goes to (Claude recommends one)</div>
  <div className="mt-1 flex flex-wrap gap-3">
    <a href="https://gemini.google.com" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ Gemini Nano Banana</a>
    <a href="https://chatgpt.com" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ gpt-image-2</a>
    <a href="https://ideogram.ai" target="_blank" rel="noreferrer" className="font-mono text-xs text-fd-muted-foreground no-underline hover:text-emerald-600">→ Ideogram</a>
  </div>
</div>

## Why this works

You don't need to spec a hero image like a creative director. Your 4-box already says who the customer is, what the offer is, and what voice the brand has. Claude reads it, makes the calls (mood, palette, photo style, composition), and outputs one image-gen prompt you paste into Nano Banana. If there's a real fork like "people or no people," Claude asks one question and stops.

The prompt below is short on purpose. No mood adjective banks, no hex pickers, no curated palettes. Those create decisions where there shouldn't be any.

This runs in Claude Project, not Gemini. The OUTPUT goes to the image tool.

## Before you run this

<div className="my-4">
  <PersistentCheckbox id="hero-prereq-1" label="Have your 4-box ready (in Project knowledge or pasteable)" />
  <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">From <a href="/docs/prompts/four-box" className="text-emerald-600 hover:underline dark:text-emerald-400">Prompt 2</a>. Already in Project knowledge if you saved it there.</p>
</div>

## Prompt

```txt
I want a hero image for my landing page. Read 4-box.md from this Project and synthesize a single image-gen prompt I can paste into Gemini Nano Banana or gpt-image-2.

Pick mood, palette, photo style, and composition yourself based on what fits the niche. The 4-box has the customer, the offer, and the voice; that's enough to design from. Don't ask me about hex codes or photo-style names.

Ask me ONE question only if there's a real fork. The usual one is "people in the frame, or object-only?" If the answer is obvious from the 4-box, skip the question and decide.

Avoid SaaS-marketing slop: no laptops, no abstract gradients with floating UI cards, no people pointing at screens. Treat it like the cover of an editorial magazine for the niche, not a stock-photo site.

Output the final image-gen prompt as ONE code block. Then tell me which tool to paste it into:
- Gemini Nano Banana Standard (100/day): default, no text in image
- gpt-image-2 in ChatGPT or Ideogram: when the hero needs text in the image

If the first generation misses, change ONE input and re-run. Don't pile adjectives.
```

## After Claude outputs the image prompt

<div className="my-4 space-y-3">
  <div>
    <PersistentCheckbox id="hero-after-1" label="Step 1. Open the recommended image tool" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground"><a href="https://gemini.google.com" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">Gemini</a>, <a href="https://chatgpt.com" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">ChatGPT</a>, or <a href="https://ideogram.ai" target="_blank" rel="noreferrer" className="text-emerald-600 hover:underline dark:text-emerald-400">Ideogram</a>. Claude will tell you which based on whether your hero needs text in the image.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-2" label="Step 2. Paste the image-gen code block as a single message" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">One message, no follow-up commentary. Let the tool generate.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-3" label="Step 3. Generate the image" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">If it misses, change ONE input (subject, composition, or photo style) and re-run. Don't pile new adjectives on.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-4" label="Step 4. Download the image and save it next to your index.html" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Save the file inside the same folder you'll use for your landing page (so it sits next to <code>index.html</code> when you build the page in Prompt 17).</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-after-5" label="Step 5. Hand off to Prompt 17" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">When you run <a href="/docs/prompts/bolt-build" className="text-emerald-600 hover:underline dark:text-emerald-400">Prompt 17 Build prompt</a>, tell the synthesizer chat you have a hero image file ready. For Path A (ChatGPT), you'll reference it as a relative path in the HTML. For Path C (Bolt), you'll attach it via the paperclip.</p>
  </div>
</div>

## Multi-option flow (when you want options to pick from)

For a hero shot you'll live with, generating 2 to 3 options and having Claude critique against the brief beats one-shot generation. Costs more credits but the picked option is usually meaningfully better.

<div className="my-4 space-y-3">
  <div>
    <PersistentCheckbox id="hero-multi-1" label="Step 1. Generate 2 to 3 options from the same prompt" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Same prompt, multiple runs in Nano Banana Standard (or fan out across Nano Banana + Ideogram if you want different model angles). Save all of them.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-multi-2" label="Step 2. Bring all options back to Claude Project, ask for a critique" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Attach the images to the same Claude chat where you synthesized the prompt. Ask: "Critique these against the brief I gave you. Which one matches the mood and composition cleanest? Which fails and why?" Claude has the brief context already, so the critique is grounded.</p>
  </div>
  <div>
    <PersistentCheckbox id="hero-multi-3" label="Step 3. Pick the winner, archive the rest" />
    <p className="ml-7 mt-1 text-sm text-fd-muted-foreground">Trust Claude's pick unless you have a strong gut reason to override. The losers archive into a folder; some become Day 2 distribution image candidates.</p>
  </div>
</div>

## What good looks like

One Claude chat, 1 to 2 turns, image-gen prompt in one code block. One Gemini run, image in hand under 5 minutes. File saved to disk, ready to attach when you run Prompt 17.

## What bad looks like

Skipping the synthesizer and writing the image prompt by hand, getting a generic stock-photo result, then burning 5 Nano Banana runs trying to fix it with adjective piles. The fix is upstream: change the photo-style input or the subject, don't pile descriptors.

## Next

Hero image file saved to disk, run [Prompt 17 Build prompt](/docs/prompts/bolt-build). Claude will ask for it when synthesizing the build prompt.
